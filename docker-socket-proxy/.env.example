# Copy to .env and fill in. .env is gitignored.

# The host's docker group id — the container runs as nobody:DOCKER_GID so the
# bind-mounted /var/run/docker.sock is readable without root.
#   getent group docker | cut -d: -f3
DOCKER_GID=999
